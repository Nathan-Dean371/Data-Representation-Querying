// Functional component for displaying the header
const Header = () => {
  return (
    // Displaying a header text
    <h1>My Header in another component</h1>
  );
};

// Exporting the Header component as the default export
export default Header;