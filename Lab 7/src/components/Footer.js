// Functional component for displaying the footer
const Footer = () => {
  return (
    // Displaying a footer text
    <h3>My Footer in another component</h3>
  );
};

// Exporting the Footer component as the default export
export default Footer;